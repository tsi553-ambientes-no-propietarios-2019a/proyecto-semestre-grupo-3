{% extends 'base.html.twig' %}

{% block title %}Hello AnuncionpetController!{% endblock %}

{% block body %}
{#para asignar variables dentro del twig se lo hace de manera mediente el set como e muestra en la siguiente linea#}
{% set variablePrueba ='esto es una prueba de variable'%}

{#para realizar una impresion de la variable anteriarmente creada "variable prueba" se le hace como se meuestra en la siguiente linea#}
{{variablePrueba}}<br>

{#Lamado desde la funcion principal "index"#}
{{SumaEntreNumero1y2}}<br>

{#condicion en twig#}
{%if Num1 > Num2%}
	Numero 1 es mayor
	{%else%}
	Numero 2 es mayor
{%endif%}

{#En la linea de abajjo se realiza un llamado a un enlace externo o interno #}
<a href="{{path('catepet')}}"> Clic aqui para redirigir al controlador categoria de mascota </a>
{% endblock %}
