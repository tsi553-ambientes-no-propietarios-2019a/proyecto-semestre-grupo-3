{{ form_start(form) }}
    {{ form_widget(form) }}

    <div class="container">
  <div class="row">
    <div class="col-sm">
    </div>
    <div class="col-sm">
    <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
    </div>
    <div class="col-sm">
    </div>
  </div>
</div>

{{ form_end(form) }}
